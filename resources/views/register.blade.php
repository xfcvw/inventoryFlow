<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>InventoryFlow | Create account</title><link rel="stylesheet" href="{{ asset('assets/app.css') }}"></head>
<body class="auth-body"><div class="language-selector"><select id="language-select"><option value="en">🇺🇸 English</option><option value="pt">🇧🇷 Português</option></select></div><main class="login-page"><section class="login-container"><div class="login-brand"><div class="logo"><div class="logo-icon">IF</div><span>InventoryFlow</span></div><div class="brand-content"><span class="brand-tag">MULTI-TENANT SaaS</span><h1>{{ $invitation ? 'Join '.$invitation->workspace->name : 'Create your workspace. Run your inventory.' }}</h1><p>{{ $invitation ? 'You were invited as '.$invitation->role.'.' : 'Products, orders, customers and stock are isolated by workspace.' }}</p></div><div class="brand-footer"><span>Laravel + PostgreSQL</span><span>Complete SaaS MVP</span></div></div><div class="login-form-area"><div class="form-container"><div class="form-header"><span class="welcome-text">START FREE</span><h2>Create your account</h2><p>{{ $invitation ? 'Use the invited email address.' : 'The first account becomes workspace owner.' }}</p></div><form method="POST" action="{{ route('register.store') }}">@csrf@if($invitation)<input type="hidden" name="invite_token" value="{{ $invitation->token }}">@endif<div class="input-group"><label>Name</label><input name="name" value="{{ old('name') }}" required></div>@unless($invitation)<div class="input-group"><label>Workspace name</label><input name="workspace_name" value="{{ old('workspace_name') }}" required></div>@endunless<div class="input-group"><label>Email</label><input type="email" name="email" value="{{ old('email', $invitation?->email) }}" {{ $invitation ? 'readonly' : '' }} required></div><div class="input-group"><label>Password</label><input type="password" name="password" required></div><div class="input-group"><label>Confirm password</label><input type="password" name="password_confirmation" required></div>@if($errors->any())<div class="server-error">{{ $errors->first() }}</div>@endif<button class="login-button">{{ $invitation ? 'Create account & join' : 'Create workspace' }}</button></form><p class="auth-switch">Already have an account? <a href="{{ route('login') }}">Sign in</a></p></div></div></section></main><script src="{{ asset('assets/i18n.js') }}"></script></body></html>