<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>InventoryFlow | Reset password</title><link rel="stylesheet" href="{{ asset('assets/app.css') }}"></head>
<body class="auth-body"><main class="simple-auth-card"><a class="sidebar-logo" href="{{ route('login') }}"><span class="logo-icon">IF</span><span>InventoryFlow</span></a><h1>Reset password</h1><form method="POST" action="{{ route('password.update') }}">@csrf<input type="hidden" name="token" value="{{ $token }}"><div class="input-group"><label>Email</label><input type="email" name="email" value="{{ old('email', $email) }}" required></div><div class="input-group"><label>New password</label><input type="password" name="password" required></div><div class="input-group"><label>Confirm password</label><input type="password" name="password_confirmation" required></div>@foreach($errors->all() as $error)<p class="form-error">{{ $error }}</p>@endforeach<button class="primary-button full-button">Reset password</button></form></main></body></html>
