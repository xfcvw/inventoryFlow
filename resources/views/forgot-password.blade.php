<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>InventoryFlow | Forgot password</title><link rel="stylesheet" href="{{ asset('assets/app.css') }}"></head>
<body class="auth-body"><main class="simple-auth-card"><a class="sidebar-logo" href="{{ route('login') }}"><span class="logo-icon">IF</span><span>InventoryFlow</span></a><h1>Forgot password</h1><p>Enter your email. In local development the reset email appears in Mailpit.</p>@if(session('status'))<div class="success-box">{{ session('status') }}</div>@endif
<form method="POST" action="{{ route('password.email') }}">@csrf<div class="input-group"><label>Email</label><input type="email" name="email" value="{{ old('email') }}" required></div>@error('email')<p class="form-error">{{ $message }}</p>@enderror<button class="primary-button full-button">Send reset link</button></form><a class="text-link" href="{{ route('login') }}">Back to login</a></main></body></html>
