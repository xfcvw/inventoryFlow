<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>InventoryFlow | Invitation</title><link rel="stylesheet" href="{{ asset('assets/app.css') }}"></head>
<body class="auth-body"><main class="simple-auth-card"><a class="sidebar-logo" href="/"><span class="logo-icon">IF</span><span>InventoryFlow</span></a><span class="eyebrow">WORKSPACE INVITATION</span><h1>{{ $invitation->workspace->name }}</h1><p>You were invited as <strong>{{ $invitation->role }}</strong> using <strong>{{ $invitation->email }}</strong>.</p>
@auth<form method="POST" action="{{ route('invitation.accept', $invitation->token) }}">@csrf<button class="primary-button full-button">Accept invitation</button></form>@else<div class="modal-actions"><a class="primary-button" href="{{ route('register', ['invite' => $invitation->token]) }}">Create account</a><a class="ghost-button" href="{{ route('login') }}">Sign in</a></div>@endauth</main></body></html>
